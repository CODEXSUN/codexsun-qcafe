# Platform module rules

Use flat module files: module, service, repository, routes, events, migration,
worker, seed, sync, types, and public index. Each table and business rule has
one module owner. Modules communicate only through public contracts and events.
